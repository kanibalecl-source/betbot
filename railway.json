{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "sh -c 'until grep -qs \" /data \" /proc/mounts; do sleep 1; done; id; ls -ld /data; echo PERSISTENT_DATA_DIR=$PERSISTENT_DATA_DIR; touch /data/.railway_probe && echo PROBE_OK && rm -f /data/.railway_probe; exec python -u app_launcher.py'",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
